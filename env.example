# ===========================================
# CONFIGURAÇÕES DO BANCO DE DADOS
# ===========================================

# URL de conexão com o PostgreSQL (Neon Cloud)
# Formato: postgresql://usuario:senha@host:porta/nome_do_banco
DB_URL=postgresql://neondb_owner:npg_5m3pUxFNWStB@ep-wandering-meadow-adx8oa1k-pooler.c-2.us-east-1.aws.neon.tech:5432/neondb?sslmode=require

# Variáveis individuais do Neon (alternativa à DB_URL)
PGHOST=ep-wandering-meadow-adx8oa1k-pooler.c-2.us-east-1.aws.neon.tech
PGDATABASE=neondb
PGUSER=neondb_owner
PGPASSWORD=npg_5m3pUxFNWStB
PGSSLMODE=require
PGCHANNELBINDING=require

# ===========================================
# CONFIGURAÇÕES DE AUTENTICAÇÃO
# ===========================================

# Chave de API para autenticação dos endpoints
# IMPORTANTE: Altere esta chave em produção para uma chave segura
API_KEY=your_secure_api_key_here

# ===========================================
# CONFIGURAÇÕES DO DOCKER (OPCIONAL)
# ===========================================

# Configurações específicas para Docker Compose
# Estas variáveis são usadas pelo docker-compose.yml

# Credenciais do PostgreSQL para Docker
POSTGRES_DB=ipca_db
POSTGRES_USER=ipca_user
POSTGRES_PASSWORD=ipca_password

# Configurações do PgAdmin (opcional)
PGADMIN_DEFAULT_EMAIL=admin@ipca.com
PGADMIN_DEFAULT_PASSWORD=admin123

# ===========================================
# CONFIGURAÇÕES DA APLICAÇÃO
# ===========================================

# Porta da aplicação (padrão: 8000)
APP_PORT=8000

# Host da aplicação (padrão: 127.0.0.1)
APP_HOST=127.0.0.1

# Ambiente (development, production)
ENVIRONMENT=development

# ===========================================
# CONFIGURAÇÕES DE LOG
# ===========================================

# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Diretório para arquivos de log
LOG_DIR=./logs
